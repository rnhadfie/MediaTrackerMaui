<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiApp1.Front.Components.Collections.CollectionDetailView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp1.Front.Components.Shared"
    xmlns:maui="clr-namespace:Microsoft.Maui;assembly=Microsoft.Maui"
    Title="CollectionDetailView">
    <VerticalStackLayout>
        <Label
            HorizontalOptions="Center"
            Style="{StaticResource Headline}"
            Text="Collections"
            VerticalOptions="Center" />
        <ImageButton
            Clicked="ImageButton_Clicked"
            HorizontalOptions="End"
            Source="editing.png"
            WidthRequest="25" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <VerticalStackLayout Grid.Column="0">
                <local:ItemGrid
                    x:Name="CollectionsDetailView"
                    HorizontalOptions="Fill"
                    VerticalOptions="Fill" />
            </VerticalStackLayout>
        </Grid>
    </VerticalStackLayout>
</ContentPage>